# OAP approved artwork

Place the two **original PNG files** from the Founder-approved artwork ZIP in this directory without renaming or recompressing them:

- `enter_my_world_wallpaper.png` — still wallpaper behind the existing password form.
- `smi_global_intelligence_command_centre.png` — approved SMI command-room background.

Image asset presence and checksums must pass the governed release gate before PR #449 is merged or deployed. The design's baked-in numbers are not runtime evidence.
