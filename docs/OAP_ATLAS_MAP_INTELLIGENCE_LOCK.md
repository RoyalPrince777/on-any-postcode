# OAP Atlas + Map Intelligence Lock

Status: LIVE LOCK TARGET
Owner: Human Authority / Founder
Surface: Public OAP World + Private SMI Command / War Room
Date: 2026-09-05

## Naming

Public product name: OAP Atlas
Private intelligence name: Map Intelligence
Combined surface: OAP Atlas + Movement Intelligence + OAP Direct
Legacy public wording: Maps / Weather / Travel may remain as quiet compatibility only.
Legacy route: /the-spot/maps-weather-travel may remain so old links do not break.

Do not create noisy duplicate names.
Do not expose "checkpoint" language on the public surface.
Do not expose internal proof-runner lane language to normal users.

## Meaning

OAP Atlas is the public spatial dashboard for ON ANY POSTCODE.
It shows the world through OAP's own hierarchy:

```text
Earth
└── Continent
    └── Country
        └── County / Region
            └── Borough / District
                └── Postcode
                    └── The Spot
```

Map Intelligence is the private SMI brain layer that checks location hierarchy, source health, stale-data boundaries, privacy, route evidence and no-fake-live-map claims.

## OAP style dashboard

The public dashboard must feel like OAP, not a generic map page:

- black/gold command-card styling where appropriate
- signal lights for map confidence
- postcode-first entry
- continent-to-postcode ladder
- The Spot connection
- Movement connection
- OAP Direct connection
- Weather / safety / travel awareness where proven
- clear "source pending" or "building" labels when proof is missing
- no hidden tracking
- no fake live route claims
- no Google dependency claim unless explicitly configured and approved

## Live map truth rule

OAP Atlas may say "live" only for the page/service being live.
It must not claim live routes, live traffic, live weather or live place data unless the relevant source, timestamp and proof are present.

Green requires:

1. Route exists.
2. Page loads.
3. Source is named.
4. Timestamp is present.
5. Stale-data status is shown.
6. Location hierarchy is correct.
7. Privacy boundary is active.
8. No hidden tracking.
9. Green Gate reads the evidence.

## Current truth board

```text
OAP Atlas naming:              GREEN / locked
Public route compatibility:    GREEN / keep old route quietly
Continent-to-postcode model:   GREEN / present in app foundation
OAP style dashboard rules:     GREEN / locked
Actual live map UI:            BUILDING until route/page proof is captured
Map source-health evidence:    BUILDING until source/timestamp/stale proof exists
Live route/traffic claim:      LOCKED until provider/source proof exists
Hidden tracking:               BLOCKED
Fake live-map claim:           BLOCKED
```

## War Room placement

```text
SMI Command
└── War Room
    └── Map Intelligence
        ├── OAP Atlas status
        ├── source health
        ├── hierarchy proof
        ├── route evidence
        ├── stale-data guard
        ├── privacy guard
        ├── Movement link
        ├── Direct link
        └── Green Gate result
```

## Final lock

Public users see OAP Atlas.
The War Room sees Map Intelligence.
Old Maps / Weather / Travel language remains quiet compatibility only.
No fake green.
No hidden tracking.
No noisy checkpoint language.
