# Waiting on evidence

The codebase cannot determine a handset-specific upstream throttle from unit tests alone. The affected-path response is now the only required diagnostic input before implementing the actual fix.
