# Final gate

Do not merge or deploy solely because source changes exist. Required evidence: branch diff review, CI pass, and supported-device runtime verification for the new capture controls. Production remains unchanged until those gates are satisfied and deployment is separately authorized.
