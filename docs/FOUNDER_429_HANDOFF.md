# Handoff

Input required: output from `scripts/probe_founder_429.sh` on the affected handset. With that output, choose the exact boundary repair; without it, changing authentication or adding more routes would be guesswork.
