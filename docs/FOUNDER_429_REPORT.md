# Diagnostic report

Finding: existing application recovery GET has no demonstrated app-level limiter responsible for the handset 429. Candidate alternate path in PR #217 shares the same application handler. Therefore the unresolved variable is upstream path discrimination versus a broader upstream key. The prepared handset probe is designed to resolve that variable without authentication data.
