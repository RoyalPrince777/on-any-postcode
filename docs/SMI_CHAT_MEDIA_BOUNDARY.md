# SMI Chat media boundary

SMI Chat captures interaction context; it does not become a second Studio.

Camera and screen sharing in this bounded slice capture a single still frame through native browser permission. The stream is stopped immediately. The frame then uses the existing SMI image contract so established media/Studio processing can handle it.

Continuous live screen/video understanding is not claimed by this slice.
