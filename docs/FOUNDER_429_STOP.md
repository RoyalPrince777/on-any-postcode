# Stop condition

If both compared paths return 429 from the same upstream signature, stop path-level changes. Escalate investigation to the shared upstream boundary rather than creating additional aliases.
