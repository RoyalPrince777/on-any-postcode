# Next action

Run `sh scripts/probe_founder_429.sh https://oap-smi.onrender.com` from the affected handset/Termux path.

This is deliberately a manual affected-path test: CI or a cloud runner cannot establish whether the handset's upstream edge condition is reproduced.

Do not enter a Founder credential during this probe. Preserve the returned status and selected headers as the evidence gate before approving any alternate entry route.
