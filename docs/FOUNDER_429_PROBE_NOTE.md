# Probe note

The alternate path may currently return 404 in production because PR #217 is held and unmerged. That result is expected and must not be mistaken for evidence that the alternate route bypasses the 429. The existing-route response still provides useful boundary evidence now.
