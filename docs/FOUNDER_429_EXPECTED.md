# Expected evidence

The useful output is deliberately small: HTTP status and any Retry-After, Server, Via, request-id, CF-Ray, or Render response headers. This is enough to distinguish common boundary ownership without collecting credentials or private dashboard content.
