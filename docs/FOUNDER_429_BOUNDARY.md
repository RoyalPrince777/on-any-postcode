# Boundary test

A cloud CI request cannot reproduce a handset/IP-specific upstream limiter. The decisive request must originate from the same handset/network path where the incident occurs. CI verifies that the diagnostic itself stays bounded and credential-free.
