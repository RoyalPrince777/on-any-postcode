# Test plan

CI should run the canonical controller tests and the SMI Chat capture contract tests. Runtime verification should then confirm microphone permission/stop behavior, camera capture and track shutdown, screen capture and track shutdown, existing attachment submission, governed SSE streaming, and Stop abort behavior on a supported browser/device.
