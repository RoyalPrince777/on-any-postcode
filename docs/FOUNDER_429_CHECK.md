# Final check

Required evidence is intentionally non-sensitive. Return the two HTTP status lines and any Retry-After/Server/Via/request-id style headers printed by the probe; do not return cookies or credentials.
