# OAP governed lint settings.
# Match the existing Ruff default safety gate: syntax/runtime error families
# plus Pyflakes. Do not expand this PR into a legacy style cleanup.

[lint]
select = ["E4", "E7", "E9", "F"]
